name: Create Cluster
run-name: Creating Cluster
on: [push]
jobs:
  SpinUpCluster:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Helm
        uses: ./.github/actions/setup-helm

      - name: Set up Kind
        uses: ./.github/actions/kind

      # - name: Decrypt secrets
      #   uses: ./.github/actions/sops
